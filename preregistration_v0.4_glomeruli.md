# 裁决实验统计预注册 v0.4（b 路：肾小球粒度升级）——跑前锁定

> 2026-09-21 | 接预注册 v0.3 §三.4"通道=像素群体不变（b 路粒度升级挂账）"。
> **唯一变量=编码侧通道粒度**：像素群体（v0.2 口径）→ 肾小球亮斑 ROI。统计框架与 v0.3 层 1 逐字一致（CID 级真对齐 15 对），判定互不挪线（v0.3 层 1/层 2 已判结果不因本批改动）。

## 〇、操作化（跑前锁定，参数零事后调节）

**ROI 检测**（每鼠在其自身基准图独立进行）：
1. 基准图 = 前 30 试验帧均值图（pipeline_v0_2.load_mean_stack 逐字复用，含 SKIP_FIRST_FRAMES=4）；
2. 粗尺度区域：高斯平滑 σ=4 → Otsu 阈值 → 亮区掩码（v0.2 build_channels 同款，开运算+填洞）——嗅球区域定义与 v0.2 逐字同源；
3. 细尺度峰值：高斯平滑 σ_det=1.2（肾小球亮斑 2-4px 的匹配尺度）→ `peak_local_max(min_distance=3, threshold_abs=Otsu(σ_det 图), labels=亮区)`——细尺度阈值由同图 Otsu 自洽给出，粗尺度定区域、细尺度定峰，无手工常数；
4. ROI = 峰值中心 3×3 方块（原始分辨率，与斑尺寸匹配）。

**锁定新参数仅三个**：σ_det=1.2 / min_distance=3 / ROI 尺寸 3×3。敏感性对照（跑后记录，不进判定）：S1 = σ_det 1.8 + 圆盘半径 1.5；S2 = 活性过滤（ΔF/F 跨试验 std 后 30% ROI 剔除——主分析**不滤**，防数据驱动剔除）。

**响应度量**（v0.2 逐字）：每试验帧均值 / 该 ROI 跨试验中位数基线（f0，onset 无关）→ R_glm[roi, odor] 试验平均。
**D_neural**：气味对距离 = 1 − pearson(R[:,i], R[:,j])（v0.1/v0.3 锁定口径）。
**D_behavior**：官方 calc_distance pool2d=True，跨鼠逐元素中位（v0.3 逐字）。
**对齐**：预注册 v0.3 §〇 CID 级对齐总表逐字沿用（odorset1 3 对 / odorset2 6 对 / odorset3 6 对，合并 15 对）。

## 一、判据（跑前锁定，与 v0.3 层 1 同款不新发明）

- **M1''（主）**：train_1952 肾小球粒度 × 行为中位距离，15 真对齐对，Spearman ρ + 置换（双尾参考值）——支持：ρ>0 且 p<0.05；判伪：ρ≤0；其余中间带如实；
- **M2''**：test_1953 泛化鼠同款（只记录）；
- **C1''**：两成像鼠编码距离向量跨鼠可靠性（15 对，只记录）；
- **对照锚点**（像素群体 v0.3 层 1 已判值）：M1' 合并 +0.043 / odorset3 +0.943 / M2' +0.314 / C1' +0.564。

**解读预登记（跑前写明，双向如实）**：粒度升级若合并 ρ 相对锚点上升（尤其 odorset3 强信号是否保持/增强）→ "编码组织在肾小球尺度"获支持面；若无改善或下降 → 像素群体已承载可用共变信息、粒度非当前瓶颈（负结果同样是操作化链信息，如实判定不补救）。

## 二、诚实边界（跑前声明）

1. 宽场 256px 下单肾小球仅 2-4px：本操作化的 ROI 是"亮斑尺度单元"，**非解剖学肾小球边界**，措辞全程用"肾小球粒度/亮斑 ROI"不用"肾小球分割"；
2. 15 对为趋势级，禁显著性宣称语气（p 只作排列参考值）；
3. 两鼠 ROI 数量可以不同（各自基准图独立检测），可比性在距离向量层面（15 维），不在逐 ROI 对应；
4. 本预注册先于管线运行落盘；只裁 b 路粒度问题，v0.3 已判层 1/层 2 不动；
5. 管线与统计脚本独立落盘（pipeline_v0.4_glomeruli.py / mantel_v0.4.py），不留会话内嵌（裁决脚本未存档教训第二次执行）。

---
*裁决实验统计预注册 v0.4 | 2026-09-21 | b 路=通道粒度唯一变量升级；参数三件锁定；判据与 v0.3 层 1 同款；双向解读预登记。*
